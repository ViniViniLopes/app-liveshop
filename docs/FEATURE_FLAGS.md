# Feature Flags

Feature flags allow safe rollout, beta access and per-tenant overrides.

Use flags for live studio, social channels, autopost channels, custom domains, push, AI and billing-related features.
